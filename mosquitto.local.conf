# Local Mosquitto config for LAN access (non-admin friendly).
# Run with: "C:\Program Files\Mosquitto\mosquitto.exe" -c "<this file>" -v

# Use a non-default port because the Windows service already binds 127.0.0.1:1883
listener 1884 0.0.0.0
allow_anonymous true
